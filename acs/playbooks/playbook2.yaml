---
 - name: this is second playbook
   hosts: all
   become: yes
   tasks:
           - name: create user
             user:
                  name: mohan
                  state: present
                  group: aws
